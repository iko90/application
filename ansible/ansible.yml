- name: exzample
  gather_facts: false
  hosts: [all]
  become: yes

  tasks:

    - name: Create conteiner
      docker_container:
        name: indexwork
        image: iko90/indexwork:latest
        pull: true
        state: started
        ports:
          - "80:80"